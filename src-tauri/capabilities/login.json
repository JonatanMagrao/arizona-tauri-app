{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "login",
  "description": "Minimal capability for passwordless activation and MFA.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize"
  ]
}
