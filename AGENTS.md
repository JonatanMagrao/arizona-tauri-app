# Agent Notes

- After running Rust checks or builds with a temporary target directory, remove generated folders such as `src-tauri/target-codex/` before finishing the task.
- If Windows file locks or permissions block cleanup, mention the remaining folder clearly in the final response and do not leave it unexplained as Git noise.
